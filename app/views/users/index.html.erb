<h1>Chefs</h1>
<p>Bekijk hier alle chefs die lid zijn van Altijd Heerlijk. <strong> Tip: </strong> volg jouw favoriete chef en ontdek heerlijke nieuwe recepten.</p>
<% if user_signed_in? %>  
<div id="pins" class="transitions-enabled">
  <% @user.each do |user| %>
    <div class="box panel panel-default">
          <div class="panel-body">
                <div class="actions">
               	<strong><p><%= user.name %></p></strong>
                <p><%= link_to image_tag(user.image_file_name), user %></p>
                <p>Aantal recepten: <strong><%= user.pins.count %></strong></p>
                <p>Aantal favoriete recepten:
                <b> </b> </p>
                <p><%= link_to 'Bekijk profiel van deze chef', user_path(user) %></p>
                </div>
    </div>
    </div>
  <% end %>
</div>
<% else %>
<%= render 'pages/login' %>
<% end %>

