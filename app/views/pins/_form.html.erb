<div class="panel panel-default">
  <div class="panel-heading">

    <%= form_for @pin, html: {multipart: true} do |f| %>
      <% if @pin.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

          <ul>
          <% @pin.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      

      <div class="form-group">
        <%= f.label :description, "Naam gerecht" %>
        <%= f.text_field :description, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :ingredients, "Ingrediënten" %>
        <%= f.text_field :ingredients, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :method, "Bereidingswijze" %>
        <%= f.text_field :method, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.label :image, "Foto" %>
        <%= f.file_field :image, class: "form-control" %>
      </div>

      <div class="form-group">
        <%= f.submit "Opslaan ", class: "btn btn-info" %>
      </div>
    <% end %>

</div>
</div>